class outputFeatures():
    def __init__(self, unique_id, sentence, sentVec, tokenVec):
        self.unique_id = unique_id
        self.sentence = sentence
        self.sentVec = sentVec
        self.tokenVec = tokenVec
